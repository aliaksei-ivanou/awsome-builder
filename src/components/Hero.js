import React from "react";

const Hero = () => (
  <div className="text-center hero my-5">
    <h1 className="mb-4">Wholesale Widget Ordering Application</h1>

    <p className="lead">
      This is a sample application that demonstrates AWS capabilities
    </p>
  </div>
);

export default Hero;
